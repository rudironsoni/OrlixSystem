# Retained only for explicit manual `make ... ARCH=orlix vmlinux` Linux
# Kbuild compatibility experiments. Normal Orlix proof does not build or
# consume this linked-image path.
extra-y += vmlinux.lds

obj-y += cpuinfo.o
obj-y += irq.o
obj-y += ptrace.o
obj-y += reboot.o
obj-y += setup.o
obj-y += process.o
obj-y += time.o
obj-y += traps.o
