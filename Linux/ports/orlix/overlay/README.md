# Orlix Linux Overlay

This directory is overlaid onto the disposable upstream Linux worktree by `make prepare-linux-worktree`.

Committed port files belong under Linux-native paths such as `arch/orlix` and `drivers/orlix`.
