diff --git a/src/selinux_internal.h b/src/selinux_internal.h
--- a/src/selinux_internal.h
+++ b/src/selinux_internal.h
@@ -17,6 +17,13 @@ extern int selinux_page_size ;
 #pragma weak pthread_key_delete
 #pragma weak pthread_setspecific
 
+#ifdef LIBSELINUX_USE_STRONG_PTHREAD_ONCE
+/* Some libcs expose pthread_once_t as a struct. The weak no-pthreads
+ * fallback below is only valid for scalar pthread_once_t implementations.
+ */
+#define __selinux_once(ONCE_CONTROL, INIT_FUNCTION)	\
+	pthread_once (&(ONCE_CONTROL), (INIT_FUNCTION))
+#else
 /* Call handler iff the first call.  */
 #define __selinux_once(ONCE_CONTROL, INIT_FUNCTION)	\
 	do {						\
@@ -28,6 +35,7 @@ extern int selinux_page_size ;
 		}					\
 	} while (0)
+#endif
 
 /* Pthread key macros */
 #define __selinux_key_create(KEY, DESTRUCTOR)			\
