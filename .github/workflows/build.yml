name: CI

on:
  push:
    branches: [main]
    tags:
    - 'v*'
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build OrlixKernel
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Show toolchain version
      run: |
        xcodebuild -version
        swift --version

    - name: Run Milestone 1 contract checks
      run: |
        if command -v rtk >/dev/null 2>&1; then
          rtk make test-milestone1-contract
          rtk make test-bootloader-contract
        else
          make test-milestone1-contract
          make test-bootloader-contract
        fi

    - name: Report build status
      run: echo "Build completed successfully"

  # Release job only runs on tags
  release:
    name: Build and Release XCFrameworks
    runs-on: macos-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Run Milestone 1 contract checks
      run: |
        if command -v rtk >/dev/null 2>&1; then
          rtk make test-milestone1-contract
          rtk make test-bootloader-contract
        else
          make test-milestone1-contract
          make test-bootloader-contract
        fi

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body_path: .build/release.md
        draft: false
        prerelease: false

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body_path: .build/release.md
        draft: false
        prerelease: false

    - name: Upload ios_error.h
      id: upload_ios_error
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: ios_error.h
        asset_name: ios_error.h
        asset_content_type: text/plain

    - name: Upload commandDictionary.plist 
      id: upload_commandDictionary
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: ./Resources/commandDictionary.plist
        asset_name: commandDictionary.plist
        asset_content_type: text/plain

    - name: Upload ios_system
      id: upload-ios_system
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/ios_system.xcframework.zip
        asset_name: ios_system.xcframework.zip
        asset_content_type: application/zip
    - name: Upload awk.xcframework
      id: upload-awk
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/awk.xcframework.zip
        asset_name: awk.xcframework.zip
        asset_content_type: application/zip
    - name: Upload tar.xcframework
      id: upload-tar
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/tar.xcframework.zip
        asset_name: tar.xcframework.zip
        asset_content_type: application/zip
    - name: Upload curl_ios.xcframework
      id: upload-curl_ios
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/curl_ios.xcframework.zip
        asset_name: curl_ios.xcframework.zip
        asset_content_type: application/zip
    - name: Upload files.xcframework
      id: upload-files
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/files.xcframework.zip
        asset_name: files.xcframework.zip
        asset_content_type: application/zip
    - name: Upload shell.xcframework
      id: upload-shell
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/shell.xcframework.zip
        asset_name: shell.xcframework.zip
        asset_content_type: application/zip
    - name: Upload ssh_cmd.xcframework
      id: upload-ssh
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/ssh_cmd.xcframework.zip
        asset_name: ssh_cmd.xcframework.zip
        asset_content_type: application/zip
        
    - name: Upload ssh_cmdA.xcframework
      id: upload-sshA
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/ssh_cmdA.xcframework.zip
        asset_name: ssh_cmdA.xcframework.zip
        asset_content_type: application/zip
        
    - name: Upload ssh_agent.xcframework
      id: upload-ssh-agent
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/ssh_agent.xcframework.zip
        asset_name: ssh_agent.xcframework.zip
        asset_content_type: application/zip

    - name: Upload sshd.xcframework
      id: upload-sshd
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/sshd.xcframework.zip
        asset_name: sshd.xcframework.zip
        asset_content_type: application/zip

    - name: Upload text.xcframework
      id: upload-text
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} 
        asset_path: .build/text.xcframework.zip
        asset_name: text.xcframework.zip
        asset_content_type: application/zip
