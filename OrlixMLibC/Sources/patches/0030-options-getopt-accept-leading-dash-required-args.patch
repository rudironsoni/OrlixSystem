diff --git i/options/internal/generic/getopt.cpp w/options/internal/generic/getopt.cpp
index 0eaef418..3a81e4bc 100644
--- i/options/internal/generic/getopt.cpp
+++ w/options/internal/generic/getopt.cpp
@@ -284,7 +284,7 @@ int getopt_common_internal(int argc, char * const argv[], const char *optstring,
 
 						if(arg[i+1]) {
 							optarg = arg + i + 1;
-						} else if(optind + 1 < argc && argv[optind + 1] && required && argv[optind + 1][0] != '-') {
+						} else if(optind + 1 < argc && argv[optind + 1] && required) {
 							/* there is an argument to this short option, separated by a space,
 								* and the shortopt specification does not specify an optional arg */
 							optarg = argv[optind + 1];
