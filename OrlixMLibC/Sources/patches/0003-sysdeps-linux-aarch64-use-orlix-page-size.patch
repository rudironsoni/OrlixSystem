diff --git i/options/internal/aarch64-include/mlibc/arch-defs.hpp w/options/internal/aarch64-include/mlibc/arch-defs.hpp
index 8bf1fd97..e78ee4dd 100644
--- i/options/internal/aarch64-include/mlibc/arch-defs.hpp
+++ w/options/internal/aarch64-include/mlibc/arch-defs.hpp
@@ -5,7 +5,11 @@
 
 namespace mlibc {
 
+#if defined(ORLIX_HOSTED_EXEC) && defined(ORLIX_HOSTED_PAGE_SIZE)
+inline constexpr size_t page_size = ORLIX_HOSTED_PAGE_SIZE;
+#else
 inline constexpr size_t page_size = 0x1000;
+#endif
 
 } // namespace mlibc
 
diff --git i/options/internal/generic/threads.cpp w/options/internal/generic/threads.cpp
index 79fc63a1..7072a905 100644
--- i/options/internal/generic/threads.cpp
+++ w/options/internal/generic/threads.cpp
@@ -4,6 +4,7 @@
 #include <frg/allocation.hpp>
 #include <frg/mutex.hpp>
 #include <mlibc/all-sysdeps.hpp>
+#include <mlibc/arch-defs.hpp>
 #include <mlibc/allocator.hpp>
 #include <mlibc/debug.hpp>
 #include <mlibc/lock.hpp>
@@ -215,7 +216,7 @@ __attribute__ ((__noreturn__)) void thread_exit(thread_exit_return ret_val) {
 }
 
 static constexpr size_t default_stacksize = 0x200000;
-static constexpr size_t default_guardsize = 4096;
+static constexpr size_t default_guardsize = page_size;
 
 int thread_attr_init(struct __mlibc_threadattr *attr) {
 	*attr = __mlibc_threadattr{};
diff --git i/options/posix/generic/pthread.cpp w/options/posix/generic/pthread.cpp
index d30f2cf5..81946883 100644
--- i/options/posix/generic/pthread.cpp
+++ w/options/posix/generic/pthread.cpp
@@ -14,6 +14,7 @@
 #include <frg/bitops.hpp>
 #include <frg/spinlock.hpp>
 #include <mlibc/all-sysdeps.hpp>
+#include <mlibc/arch-defs.hpp>
 #include <mlibc/allocator.hpp>
 #include <mlibc/debug.hpp>
 #include <mlibc/thread.hpp>
@@ -59,7 +60,7 @@ static constexpr unsigned int rc_count_mask = (static_cast<uint32_t>(1) << 31) -
 static constexpr unsigned int rc_waiters_bit = static_cast<uint32_t>(1) << 31;
 
 static constexpr size_t default_stacksize = 0x200000;
-static constexpr size_t default_guardsize = 4096;
+static constexpr size_t default_guardsize = mlibc::page_size;
 
 // ----------------------------------------------------------------------------
 // pthread_attr and pthread functions.
diff --git i/sysdeps/linux/generic/thread.cpp w/sysdeps/linux/generic/thread.cpp
index 7735c024..6771a4a5 100644
--- i/sysdeps/linux/generic/thread.cpp
+++ w/sysdeps/linux/generic/thread.cpp
@@ -1,6 +1,7 @@
 #include <mlibc/thread-entry.hpp>
 #include <mlibc/all-sysdeps.hpp>
 #include <mlibc/thread.hpp>
+#include <mlibc/arch-defs.hpp>
 #include <bits/ensure.h>
 #include <sys/mman.h>
 #include <stdint.h>
@@ -23,6 +24,13 @@ extern "C" void __mlibc_enter_thread(void *entry, void *user_arg) {
 namespace mlibc {
 
 static constexpr size_t default_stacksize = 0x200000;
+static constexpr size_t page_mask = page_size - 1;
+
+static size_t align_to_page_size(size_t value) {
+	if (!value)
+		return 0;
+	return (value + page_mask) & ~page_mask;
+}
 
 int Sysdeps<PrepareStack>::operator()(void **stack, void *entry, void *user_arg, void *tcb, size_t *stack_size, size_t *guard_size, void **stack_base) {
 	(void)tcb;
@@ -34,6 +42,7 @@ int Sysdeps<PrepareStack>::operator()(void **stack, void *entry, void *user_arg,
 		map = reinterpret_cast<uintptr_t>(*stack);
 		*guard_size = 0;
 	} else {
+		*guard_size = align_to_page_size(*guard_size);
 		map = reinterpret_cast<uintptr_t>(
 				mmap(nullptr, *stack_size + *guard_size,
 					PROT_NONE,
