diff --git i/options/internal/generic/getopt.cpp w/options/internal/generic/getopt.cpp
index 15935ff4..0eaef418 100644
--- i/options/internal/generic/getopt.cpp
+++ w/options/internal/generic/getopt.cpp
@@ -17,6 +17,13 @@ int optopt;
 
 int __optpos = 1;
 
+const char *getopt_program_name(char * const argv[]) {
+	const char *name = argv[0] ? argv[0] : "";
+	if(auto slash = strrchr(name, '/'); slash)
+		return slash + 1;
+	return name;
+}
+
 int getopt_common_internal(int argc, char * const argv[], const char *optstring, const struct option *longopts,
 		int *longindex, enum mlibc::GetoptMode mode) {
 	// find a matching longopt for an `arg` of length `n`
@@ -188,8 +195,10 @@ int getopt_common_internal(int argc, char * const argv[], const char *optstring,
 
 			auto k = longopt_find(arg, n);
 			if(std::holds_alternative<std::monostate>(k)) {
-				if(opterr)
-					fprintf(stderr, "--%s is not a valid option.\n", arg);
+				if(opterr) {
+					fprintf(stderr, "%s: unrecognized option '--%.*s'\n",
+							getopt_program_name(argv), static_cast<int>(n), arg);
+				}
 				optind++;
 				return '?';
 			} else if(std::holds_alternative<char>(k)) {
@@ -313,8 +322,10 @@ int getopt_common_internal(int argc, char * const argv[], const char *optstring,
 						the error message by setting opterr to 0. */
 					optopt = arg[1];
 					if(opterr)
-						fprintf(stderr, "%s is not a valid option.\n", arg);
+						fprintf(stderr, "%s: invalid option -- '%c'\n",
+								getopt_program_name(argv), arg[i]);
 					optind++;
+					__optpos = 1;
 					return '?';
 				}
 			}
