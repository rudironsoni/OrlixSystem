diff --git a/options/ansi/include/mlibc/file-io.hpp b/options/ansi/include/mlibc/file-io.hpp
index d963d45cd..4450477b6 100644
--- a/options/ansi/include/mlibc/file-io.hpp
+++ b/options/ansi/include/mlibc/file-io.hpp
@@ -49,4 +49,6 @@ struct StdioLock {
 	}
 };
+constexpr size_t ungetBufferSize = 8;
+
 struct abstract_file : __mlibc_file_base {
 public:
@@ -64,6 +66,8 @@ public:
 	int read(char *buffer, size_t max_size, size_t *actual_size);
 	int write(const char *buffer, size_t max_size, size_t *actual_size);
 	int unget(char c);
+	int unget_wide(wchar_t c);
+	bool read_wide_unget(wchar_t *c);
 
 	int update_bufmode(buffer_mode mode);
 
@@ -102,6 +106,10 @@ private:
 	int _save_pos();
 
 	void _ensure_allocation();
+
+	// ungetwc() pushback is independent from byte-oriented external encoding.
+	wchar_t _wide_unget_buffer[ungetBufferSize];
+	size_t _wide_unget_size = 0;
 
 	stream_type _type;
 	buffer_mode _bufmode;
diff --git a/options/ansi/generic/file-io.cpp b/options/ansi/generic/file-io.cpp
index 15ce372a7..1248faf53 100644
--- a/options/ansi/generic/file-io.cpp
+++ b/options/ansi/generic/file-io.cpp
@@ -40,9 +40,6 @@ namespace {
 	// Useful when debugging the FILE implementation.
 	constexpr bool globallyDisableBuffering = false;
 
-	// The maximum number of characters we permit the user to ungetc.
-	constexpr size_t ungetBufferSize = 8;
-
 	// List of files that will be flushed before exit().
 	file_list &global_file_list() {
 		static frg::eternal<file_list> list;
@@ -260,6 +257,21 @@ int abstract_file::unget(char c) {
 	}
 }
 
+int abstract_file::unget_wide(wchar_t c) {
+	if (_wide_unget_size >= ungetBufferSize)
+		return EOF;
+
+	_wide_unget_buffer[_wide_unget_size++] = c;
+	return 0;
+}
+
+bool abstract_file::read_wide_unget(wchar_t *c) {
+	if (!_wide_unget_size)
+		return false;
+	*c = _wide_unget_buffer[--_wide_unget_size];
+	return true;
+}
+
 int abstract_file::update_bufmode(buffer_mode mode) {
 	// setvbuf() has undefined behavior if I/O has been performed.
 	__ensure(__dirty_begin == __dirty_end
@@ -276,6 +288,7 @@ void abstract_file::purge() {
 	__valid_limit = 0;
 	__dirty_end = __dirty_begin;
 	__unget_ptr = __buffer_ptr;
+	_wide_unget_size = 0;
 }
 
 int abstract_file::post_flush() {
@@ -439,6 +452,7 @@ int abstract_file::_reset() {
 	__offset = 0;
 	__io_offset = 0;
 	__valid_limit = 0;
+	_wide_unget_size = 0;
 
 	return 0;
 }
diff --git a/options/ansi/generic/stdio.cpp b/options/ansi/generic/stdio.cpp
index 765d7d4be..d11803572 100644
--- a/options/ansi/generic/stdio.cpp
+++ b/options/ansi/generic/stdio.cpp
@@ -276,6 +276,10 @@ wint_t fgetwc_unlocked(mlibc::abstract_file *f) {
 		return WEOF;
 	}
 
+	wchar_t ungot;
+	if (f->read_wide_unget(&ungot))
+		return ungot;
+
 	// POSIX: If the EOF indicator is set, return WEOF.
 	if (f->__status_bits & __MLIBC_EOF_BIT)
 		return WEOF;
@@ -315,22 +319,11 @@ wint_t ungetwc_unlocked(wint_t c, mlibc::abstract_file *f) {
 	if (c == WEOF)
 		return WEOF;
 
-	char buf[MB_LEN_MAX];
-	mbstate_t state = { };
-	auto encoding = wcrtomb(buf, static_cast<wchar_t>(c), &state);
-	if (encoding == size_t(-1))
-		return WEOF;
-
-	for (size_t i = 0; i < encoding; i++) {
-		int ret = f->unget(buf[encoding - 1 - i]);
-		if (ret == EOF)
-			return WEOF;
-	}
+	if (f->unget_wide(static_cast<wchar_t>(c)) == EOF)
+		return WEOF;
 
 	// POSIX: A successful call to ungetwc shall clear the EOF indicator for the stream.
-	if (encoding)
-		f->__status_bits &= ~__MLIBC_EOF_BIT;
-
+	f->__status_bits &= ~__MLIBC_EOF_BIT;
 	return c;
 }
 
